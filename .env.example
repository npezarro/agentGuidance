# Agent Guidance — infrastructure config
# Copy to ~/.config/agent-guidance/.env (not committed to repo)

AG_VM_HOST=YOUR_VM_IP
AG_VM_USER=YOUR_USERNAME
AG_SSH_KEY=$HOME/.ssh/vm_key
AG_WP_PATH=/var/www/html
AG_WP_SITE=https://YOUR_DOMAIN
