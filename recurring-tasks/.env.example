# Discord webhook for #recurring-tasks channel
RECURRING_TASKS_WEBHOOK=

# Override Claude binary path (default: claude)
# CLAUDE_BIN=/usr/local/bin/claude

# Max concurrent tasks across all runners (default: 3)
# MAX_CONCURRENT_TASKS=3
